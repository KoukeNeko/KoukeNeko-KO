# This workflow is now deprecated. Please use gradle.yml instead.
# Your project uses Gradle, not Maven.

name: Java CI with Maven (Deprecated)

on:
  workflow_dispatch:
    # Allow manual execution but not automatically triggered

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Project uses Gradle
      run: echo "This project uses Gradle, not Maven. This workflow is deprecated. Please use the 'Java CI with Gradle' workflow instead."
